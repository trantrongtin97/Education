 - Abstract class is class 
		1. Can be instantiated (Create by key word new)
		2. Usually using be base class
 - Interface like a class but 
		1. All method and property are abstract. 
		2. All the methods and properties defined in Interface are by default public and abstract.
		3. Using "tach biet" giua cac tac dong ben ngoai va noi bo he thong
- Partial class is class but field, property can be define in two or more files class
		1. Using lagre project, help programmer to work on it simultaneously
		2. During compile time all the partial class are compiled into one type only
- Sealed class can be inherited 
		1. Not used as a base class
		2. Can be use end inherited on derived class